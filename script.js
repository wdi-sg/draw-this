console.log("hello script js");

// if user key-in 1, 1 emoticon will appear
var inputHappened = function(currentInput){
    var value = parseInt(currentInput);
    if (value === 1) {
    var valueOne = document.querySelector(".para1");
    display(valueOne);
    console.log(value);
    }

    if (value === 2) {
    var valueTwo = document.querySelector(".para2");
    display(valueTwo);
    console.log(value);
    }

    if (value ==== 3) {
    var valueThree = document.querySelector(".para3");
    display(valueThree);
    console.log(value);
    }

    else {
        prompt("Invalid input!");
    }

};

var emptyField = function () {
    var valueClear = !parseInt(currentInput) {
        var emptyField = document.querySelector.removeChild(.para1);
        var emptyField2 = document.querySelector.removeChild(.para2);
        var emptyField3 = document.querySelector.removeChild(.para3);
        var valueClear = !parseInt(currentInput) {
        if (currentInput.lowercase === clear) {

        }


    }



// if user key-in 1, 1 emoticon will appear

//if user key-in 3, 3 emoticons will appear

//if user key-in 2, 2 emoticons will appear while retaining the previous emoticons

//if user type clear, all emoticons will disappear s